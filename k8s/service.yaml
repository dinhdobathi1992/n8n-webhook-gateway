apiVersion: v1
kind: Service
metadata:
  name: n8n-webhook-gateway
  namespace: platformbot
  labels:
    app: n8n-webhook-gateway
spec:
  type: ClusterIP
  selector:
    app: n8n-webhook-gateway
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
