apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cloudflared-pdb
  namespace: n8n
  labels:
    app: cloudflared
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: cloudflared
