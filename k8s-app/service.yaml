apiVersion: v1
kind: Service
metadata:
  name: n8n-webhook-gateway-app
  namespace: platformbot
  labels:
    app: n8n-webhook-gateway-app
spec:
  type: ClusterIP
  selector:
    app: n8n-webhook-gateway-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
