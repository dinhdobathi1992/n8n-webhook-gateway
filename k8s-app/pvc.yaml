apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-webhook-gateway-app-data
  namespace: platformbot
  labels:
    app: n8n-webhook-gateway-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 1Gi
